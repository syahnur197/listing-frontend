import { useSession } from "next-auth/client";
import { useEffect } from "react";

export default function CheckSession() {
  //   const [session, isLoading] = useSession();

  // the plan is for me to check the access token here, in the future
  return <></>;
}
